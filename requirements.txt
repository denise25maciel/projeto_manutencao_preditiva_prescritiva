# Manutencao Prescritiva — dependencias
#
# Instalacao:  pip install -r requirements.txt
#
# Os limites inferiores sao os que o projeto realmente usa; os superiores
# travam a proxima major, onde a API pode quebrar. Ambiente validado:
# Python 3.14.4 no Windows 11.

# --- Dados -----------------------------------------------------------------
pandas>=2.2,<4
numpy>=1.26,<3
pyarrow>=15.0            # serializacao das tabelas para a UI

# --- Interface -------------------------------------------------------------
streamlit>=1.36,<2
altair>=5.3,<7           # graficos; a UI nao usa matplotlib

# --- Configuracao ----------------------------------------------------------
pyyaml>=6.0,<7           # data/fault_map.yaml (Parte 1)

# --- Documentos ------------------------------------------------------------
pypdf>=5.0,<8            # PDFs de procedimento -> Markdown

# --- Banco -----------------------------------------------------------------
sqlalchemy>=2.0,<3       # esquema do SQLite; migrar para Postgres e trocar a URL

# --- Modelo de linguagem (Parte 5) -----------------------------------------
# O `core` traz as mensagens tipadas (SystemMessage/HumanMessage/AIMessage) e a
# saida estruturada. Ele nao fala com provedor nenhum: cada adaptador e um
# pacote a parte, instalado so quando aquele provedor for usado.
#
#   pip install langchain-ollama      # a meta da entrega, roda local
#   pip install langchain-openai      # para desenvolver, exige OPENAI_API_KEY
#   pip install langchain-anthropic   # opcional
langchain-core>=1.0,<2
pydantic>=2.0,<3         # o esquema que o modelo preenche ao separar sintomas
python-dotenv>=1.0,<2    # le a chave do `.env`, que nunca vai para o git
httpx>=0.27,<1           # lista os modelos baixados no Ollama

# --- Notebooks -------------------------------------------------------------
# Fora do runtime: nem o modulo nem a UI dependem destes.
jupyterlab>=4.0,<5
matplotlib>=3.8,<4

# ---------------------------------------------------------------------------
# Entram nas proximas partes — ainda nao instalados para manter o ambiente
# leve. Estao mapeados como extras no pyproject.toml.
#
#   Parte 3   scikit-learn>=1.5
#   Parte 4   sentence-transformers
#   Parte 5   langchain-openai | langchain-ollama | langchain-anthropic
#   Parte 6   fastapi, uvicorn
# ---------------------------------------------------------------------------
